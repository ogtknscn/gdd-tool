import { useCallback, useEffect, useMemo, useState } from 'react';
import type { AppCommandId } from './commands/appCommands';
import { AppHeader } from './components/AppHeader';
import { Canvas } from './components/Canvas';
import { CommandPalette } from './components/CommandPalette';
import { CreationRail } from './components/CreationRail';
import { FeedbackHost } from './components/FeedbackHost';
import { Inspector } from './components/Inspector';
import { NodeDetailPanel } from './components/NodeDetailPanel';
import { Outline } from './components/Outline';
import { PageTabs } from './components/PageTabs';
import { PlaygroundTools } from './components/PlaygroundTools';
import { ShortcutHelp } from './components/ShortcutHelp';
import { StartScreen } from './components/StartScreen';
import { TemplateWelcome } from './components/TemplateWelcome';
import { ValidationPanel } from './components/ValidationPanel';
import type { TemplateId } from './domain/templates';
import { clearRecoverySnapshot, exportProjectPdf, loadRecentProjects, loadRecoverySnapshot, openProjectFile, openRecentProject, saveProjectFile, saveRecoverySnapshot, type RecentProject } from './domain/storage';
import { validateProject } from './domain/validation';
import type { ProjectModel } from './domain/types';
import { feedback } from './stores/feedbackStore';
import { downloadMarkdown, downloadPng, downloadSvg } from './domain/export';
import { useProjectStore } from './stores/projectStore';
import { useUiStore } from './stores/uiStore';

type SaveState = 'loading' | 'saving' | 'saved' | 'error'; type SaveResult = 'saved' | 'cancelled' | 'failed'; type WelcomeMode = 'required' | 'optional' | undefined;
export default function App() {
  const [welcomeMode, setWelcomeMode] = useState<WelcomeMode>(); const [hydrated, setHydrated] = useState(false); const [saveState, setSaveState] = useState<SaveState>('loading'); const [startOpen, setStartOpen] = useState(true); const [recovery, setRecovery] = useState<ProjectModel | null>(null); const [recent, setRecent] = useState<RecentProject[]>([]);
  const project = useProjectStore((state) => state.project); const dirty = useProjectStore((state) => state.dirty); const currentFilePath = useProjectStore((state) => state.currentFilePath); const undoStack = useProjectStore((state) => state.undoStack); const redoStack = useProjectStore((state) => state.redoStack); const outlineOpen = useUiStore((state) => state.outlineOpen); const inspectorOpen = useUiStore((state) => state.inspectorOpen); const density = useUiStore((state) => state.cardDensity); const issues = useMemo(() => validateProject(project), [project]);
  useEffect(() => { void Promise.all([loadRecoverySnapshot(), loadRecentProjects()]).then(([snapshot, items]) => { setRecovery(snapshot); setRecent(items); setSaveState('saved'); }).catch((error) => { setSaveState('error'); feedback.toast(`Kurtarma kaydı yüklenemedi: ${String(error)}`, 'error'); }).finally(() => setHydrated(true)); }, []);
  useEffect(() => { if (!hydrated) return; const timeout = window.setTimeout(() => { void saveRecoverySnapshot(project).catch(() => setSaveState('error')); }, 650); return () => window.clearTimeout(timeout); }, [hydrated, project]);
  useEffect(() => { const beforeUnload = (event: BeforeUnloadEvent) => { if (!useProjectStore.getState().dirty) return; event.preventDefault(); event.returnValue = ''; }; window.addEventListener('beforeunload', beforeUnload); return () => window.removeEventListener('beforeunload', beforeUnload); }, []);
  const save = useCallback(async (saveAs = false): Promise<SaveResult> => { setSaveState('saving'); try { const store = useProjectStore.getState(); const path = await saveProjectFile(store.project, store.currentFilePath, saveAs); if (!path) { setSaveState('saved'); feedback.toast('Kaydetme iptal edildi.'); return 'cancelled'; } store.markSaved(path); await clearRecoverySnapshot(); setSaveState('saved'); feedback.toast('Proje kaydedildi.', 'success'); return 'saved'; } catch (error) { setSaveState('error'); feedback.toast(`Proje kaydedilemedi: ${String(error)}`, 'error'); return 'failed'; } }, []);
  const guardUnsaved = useCallback(async () => { if (!useProjectStore.getState().dirty) return true; const choice = await feedback.choose({ title: 'Kaydedilmemiş değişiklikler', message: 'Devam etmeden önce değişikliklerinizi kaydedebilirsiniz.', choices: [{ value: 'save', label: 'Kaydet' }, { value: 'discard', label: 'Kaydetmeden devam', tone: 'danger' }], cancelLabel: 'İptal' }); if (choice === 'discard') return true; if (choice === 'save') return (await save()) === 'saved'; return false; }, [save]);
  const open = useCallback(async () => { if (!await guardUnsaved()) return; try { const opened = await openProjectFile(); if (opened) { useProjectStore.getState().replaceProject(opened.project, opened.path); setWelcomeMode(undefined); setSaveState('saved'); } } catch (error) { setSaveState('error'); feedback.toast(`Proje açılamadı: ${String(error)}`, 'error'); } }, [guardUnsaved]);
  const createNew = useCallback(() => { setWelcomeMode('required'); }, []);
  const run = useCallback((id: AppCommandId) => { const store = useProjectStore.getState(); const ui = useUiStore.getState(); const actions: Record<AppCommandId, () => void> = { new: createNew, open: () => void open(), save: () => void save(), saveAs: () => void save(true), undo: store.undo, redo: store.redo, templates: () => setWelcomeMode('optional'), validation: ui.toggleValidation, outline: ui.toggleOutline, inspector: ui.toggleInspector, shortcuts: () => ui.setShortcutsOpen(true), addMechanic: () => store.addNode('mechanic'), addEntity: () => store.addNode('entity'), addLevel: () => store.addNode('level'), addQuest: () => store.addNode('quest'), addUi: () => store.addNode('ui'), addAsset: () => store.addNode('asset') }; actions[id](); }, [createNew, open, save]);
  useEffect(() => { const keydown = (event: KeyboardEvent) => { const target = event.target as HTMLElement | null; const typing = target?.matches('input, textarea, select, [contenteditable="true"]'); if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === 'k') { event.preventDefault(); useUiStore.getState().setPaletteOpen(true); return; } if (!typing && event.key === '?') { event.preventDefault(); useUiStore.getState().setShortcutsOpen(true); return; } if (!(event.ctrlKey || event.metaKey)) return; const key = event.key.toLowerCase(); if (key === 's') { event.preventDefault(); run(event.shiftKey ? 'saveAs' : 'save'); } else if (key === 'o') { event.preventDefault(); run('open'); } else if (key === 'n') { event.preventDefault(); run('new'); } else if (key === 'z') { event.preventDefault(); run(event.shiftKey ? 'redo' : 'undo'); } else if (key === 'y') { event.preventDefault(); run('redo'); } }; window.addEventListener('keydown', keydown); return () => window.removeEventListener('keydown', keydown); }, [run]);
  const chooseTemplate = async (id: TemplateId) => { if (!await guardUnsaved()) return; useProjectStore.getState().applyTemplate(id); setWelcomeMode(undefined); };
  const saveLabel = saveState === 'loading' ? 'Yükleniyor' : saveState === 'saving' ? 'Kaydediliyor' : saveState === 'error' ? 'Kayıt hatası' : dirty ? 'Kaydedilmedi' : 'Kaydedildi';
  if (hydrated && startOpen) return <StartScreen recovery={recovery} recent={recent} onContinue={() => { if (recovery) useProjectStore.getState().replaceProject(recovery, undefined, true); setStartOpen(false); }} onDiscard={() => { void clearRecoverySnapshot(); setRecovery(null); }} onNew={() => { setStartOpen(false); setWelcomeMode('required'); }} onOpen={() => { setStartOpen(false); void open(); }} onRecent={(path) => { void openRecentProject(path).then((opened) => { if (opened) { useProjectStore.getState().replaceProject(opened.project, opened.path); setStartOpen(false); } }); }} />;
  return <div className="app-shell"><AppHeader title={project.title} dirty={dirty} filePath={currentFilePath} saveLabel={saveLabel} saveState={saveState} issueCount={issues.length} canUndo={Boolean(undoStack.length)} canRedo={Boolean(redoStack.length)} density={density} run={run} setDensity={useUiStore.getState().setCardDensity} onExportMarkdown={() => downloadMarkdown(project)} onExportSvg={() => downloadSvg(project)} onExportPng={() => void downloadPng(project)} /><button className="pdf-export-button" onClick={() => { void exportProjectPdf(project).then((path) => feedback.toast(path ? `PDF dışa aktarıldı: ${path}` : 'PDF dışa aktarma masaüstü uygulamasında kullanılabilir.', path ? 'success' : 'info')).catch((error) => feedback.toast(`PDF dışa aktarılamadı: ${String(error)}`, 'error')); }}>PDF dışa aktar</button><PageTabs /><div className={`workspace ${outlineOpen ? 'has-outline' : ''} ${inspectorOpen ? 'has-inspector' : ''}`}><CreationRail onTemplates={() => setWelcomeMode('optional')} />{outlineOpen && <Outline />}<Canvas />{inspectorOpen && <Inspector />}</div><ValidationPanel /><NodeDetailPanel /><CommandPalette run={run} /><ShortcutHelp />{welcomeMode && <TemplateWelcome required={welcomeMode === 'required'} onChoose={(id) => void chooseTemplate(id)} onClose={() => setWelcomeMode(undefined)} />}<FeedbackHost /></div>;
}
