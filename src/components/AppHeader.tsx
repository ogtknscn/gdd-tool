import type { AppCommandId } from '../commands/appCommands';
import type { CardDensity } from '../stores/uiStore';

type Props = { title: string; dirty: boolean; filePath?: string; saveLabel: string; saveState: string; issueCount: number; canUndo: boolean; canRedo: boolean; density: CardDensity; run: (id: AppCommandId) => void; setDensity: (density: CardDensity) => void };
export function AppHeader({ title, dirty, filePath, saveLabel, saveState, issueCount, canUndo, canRedo, density, run, setDensity }: Props) {
  return <header className="app-header"><div className="header-leading"><details className="menu"><summary aria-label="Dosya menüsü">Dosya</summary><div className="menu-popover"><button onClick={() => run('new')}>Yeni <kbd>Ctrl+N</kbd></button><button onClick={() => run('open')}>Aç <kbd>Ctrl+O</kbd></button><button onClick={() => run('save')}>Kaydet <kbd>Ctrl+S</kbd></button><button onClick={() => run('saveAs')}>Farklı kaydet <kbd>Ctrl+Shift+S</kbd></button></div></details><div className="project-identity"><h1>{title}{dirty && <span className="dirty-dot" aria-label="Kaydedilmemiş değişiklikler">●</span>}</h1><span title={filePath}>{filePath ?? 'Yerel proje'}</span></div></div><nav aria-label="Proje komutları"><span className={`save-state ${saveState} ${dirty ? 'dirty' : ''}`}>{saveLabel}</span><button aria-label="Geri al" title="Geri al (Ctrl+Z)" disabled={!canUndo} onClick={() => run('undo')}>↶</button><button aria-label="Yinele" title="Yinele (Ctrl+Y)" disabled={!canRedo} onClick={() => run('redo')}>↷</button><button className={issueCount ? 'validation-error' : ''} onClick={() => run('validation')}>Kontrol <span className="badge">{issueCount}</span></button><button className="primary" onClick={() => run('save')}>Kaydet</button><details className="menu menu-right"><summary aria-label="Diğer komutlar">•••</summary><div className="menu-popover"><button onClick={() => run('templates')}>Şablonlar</button><button onClick={() => run('outline')}>Outline paneli</button><button onClick={() => run('inspector')}>Inspector paneli</button><label>Kart ayrıntısı<select value={density} onChange={(event) => setDensity(event.target.value as CardDensity)}><option value="auto">Otomatik</option><option value="compact">Kompakt</option><option value="standard">Standart</option><option value="detailed">Detaylı</option></select></label><button onClick={() => run('shortcuts')}>Klavye kısayolları</button></div></details></nav></header>;
}
