import type { ProjectModel } from '../domain/types';
import type { RecentProject } from '../domain/storage';
import { useT } from '../stores/uiStore';
type Props = { recovery: ProjectModel | null; recent: RecentProject[]; onContinue: () => void; onDiscard: () => void; onRecent: (path: string) => void; onNew: () => void; onOpen: () => void };
export function StartScreen({ recovery, recent, onContinue, onDiscard, onRecent, onNew, onOpen }: Props) { const t = useT(); return <div className="start-screen"><section><span className="eyebrow">{t('app.title')}</span><h2>{t('startScreen.title')}</h2>{recovery && <div className="start-recovery"><h3>{t('startScreen.recoveryTitle')}</h3><p>{recovery.title}</p><button className="primary" onClick={onContinue}>{t('startScreen.continue')}</button><button onClick={onDiscard}>{t('startScreen.discard')}</button></div>}<div className="start-actions"><button className="primary" onClick={onNew}>{t('startScreen.newFromTemplate')}</button><button onClick={onOpen}>{t('startScreen.open')}</button></div>{recent.length > 0 && <div className="start-recents"><h3>{t('startScreen.recentProjects')}</h3>{recent.map((item) => <button key={item.path} onClick={() => onRecent(item.path)}><strong>{item.title}</strong><small>{item.path}</small></button>)}</div>}</section></div>; }
