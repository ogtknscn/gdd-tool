import type { ProjectModel } from '../domain/types';
import type { RecentProject } from '../domain/storage';
type Props = { recovery: ProjectModel | null; recent: RecentProject[]; onContinue: () => void; onDiscard: () => void; onRecent: (path: string) => void; onNew: () => void; onOpen: () => void };
export function StartScreen({ recovery, recent, onContinue, onDiscard, onRecent, onNew, onOpen }: Props) { return <div className="start-screen"><section><span className="eyebrow">GDD Tool</span><h2>Başlangıç alanı</h2>{recovery && <div className="start-recovery"><h3>Kurtarılabilir çalışma bulundu</h3><p>{recovery.title}</p><button className="primary" onClick={onContinue}>Çalışmaya devam et</button><button onClick={onDiscard}>Kurtarmayı at</button></div>}<div className="start-actions"><button className="primary" onClick={onNew}>Şablonla yeni proje</button><button onClick={onOpen}>Projeyi aç</button></div>{recent.length > 0 && <div className="start-recents"><h3>Son projeler</h3>{recent.map((item) => <button key={item.path} onClick={() => onRecent(item.path)}><strong>{item.title}</strong><small>{item.path}</small></button>)}</div>}</section></div>; }
