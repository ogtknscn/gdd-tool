[package]
name = "gdd-tool"
version = "0.1.0"
description = "Local-first Game Design Document workspace"
edition = "2021"

[lib]
name = "gdd_tool_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_Storage_FileSystem", "Win32_UI_Controls_Dialogs"] }
